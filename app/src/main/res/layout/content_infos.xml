<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.activities.InfosActivity"
    tools:showIn="@layout/activity_infos">


    <ImageView
        android:id="@+id/logo_app"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:src="@drawable/super_quizz_logo"
        android:layout_marginTop="100dp"
        />

    <TextView
        android:id="@+id/infos_app"
        android:text="@string/description_app"
        android:layout_below="@id/logo_app"
        android:textSize="30sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textAlignment="center"/>

    <LinearLayout
        android:id="@+id/layout_check_keep_answer_on_refresh"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/infos_app"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="20dp">
        <CheckBox
            android:id="@+id/checkbox_keep_answer_on_refresh"
            android:checked="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:text="garder les réponses après réactualisation"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </LinearLayout>

</RelativeLayout>